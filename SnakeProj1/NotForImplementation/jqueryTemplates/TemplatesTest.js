/**
 * Created by Shane on 17/03/2014.
 */

$(document).ready(function ()
{
    var gridHeight = 40;
    var gridWidth = 40;
    var board = [];
    var div = "<div></div>";




    console.log(board);


/*
    var markup = "<div class='gridNode' style='top: ${top}; left: ${left}'></div>";


    $.template("gridNodeTemplate", markup);

    for (var i = 0; i < gridHeight; i++) {
        for (var j = 0; j < gridWidth; j++) {
            $.tmpl("gridNodeTemplate", {top: (i*10).toString()+"px",left: (j*10).toString()+"px"}).appendTo("#testBoard")
        }
    };



    var row = "<div class='rowClass' id='row${rowtop}' style='top: ${top};'></div>";
    var node = "<div class='nodeClass' style='top: ${top}; left: ${left}'></div>";
    $.template("rowTemplate", row);
    $.template("nodeTemplate", node);

    for(var i=0; i<gridHeight; i++) {
        for(var j = 0; j< gridWidth; j++){
            $("#row"+i).append($.tmpl("nodeTemplate", {top: (i).toString()+"px", left: (j*10).toString()+"px"}))
           // $.tmpl("nodeTemplate", {top: (i*10).toString()+"px", left: (j*10).toString()+"px"}).append("#row"+ i.toString());
        }

    }

 */

});